<!--
<%= @talks.each do |t| %>
    <% if user_signed_in? %>
        <% if current_user.already_empathied?(t) %>
            <%= link_to talk_empathy_path(id: t.id, talk_id: t.id), method: :delete, remote: true do %>
                <i class="fas fa-heart"></i>共感<%= t.empathies.count %>
            <% end %>
        <% else%>
            <%= link_to talk_empathies_path(id: t.id, talk_id: t.id), method: :post, remote: true do %>
                <i class="far fa-heart"></i>共感<%= t.empathies.count %>
            <% end %>
        <% end %>
    <% else %>
        <i class="far fa-heart"></i><%= t.empathies.count %>
    <% end %>
<% end %>
-->